{
  "name": "tokenpulse",
  "displayName": "TokenPulse",
  "description": "Live token & cost tracker for GitHub Copilot and VS Code AI features",
  "version": "0.1.0",
  "publisher": "alpha-studio",
  "icon": "media/icon.png",
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": ["Other", "Machine Learning"],
  "keywords": ["token", "copilot", "ai", "cost", "tracker"],
  "activationEvents": ["onStartupFinished"],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "tokenpulse.showDashboard",
        "title": "TokenPulse: Show Dashboard"
      },
      {
        "command": "tokenpulse.resetSession",
        "title": "TokenPulse: Reset Session"
      }
    ],
    "configuration": {
      "title": "TokenPulse",
      "properties": {
        "tokenpulse.warnAtPercent": {
          "type": "number",
          "default": 75,
          "description": "Show warning when token usage reaches this percentage"
        },
        "tokenpulse.showStatusBar": {
          "type": "boolean",
          "default": true,
          "description": "Show token usage in the status bar"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc":  
  }
}